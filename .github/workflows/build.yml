name: Build ZMK firmware

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup dependencies
        run: |
          sudo apt update
          sudo apt install -y cmake ninja-build gperf ccache dfu-util device-tree-compiler wget python3-pip

      - name: Install west
        run: pip3 install west

      - name: Init workspace
        run: west init -l config

      - name: Update modules
        run: west update

      - name: Build firmware
        run: west build -s zmk/app -b nice_nano_v2 -- -DSHIELD=lily58_left

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: build/zephyr/zmk.uf2
